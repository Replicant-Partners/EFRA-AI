[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && ./node_modules/.bin/next start -p $PORT"
restartPolicyType = "ON_FAILURE"
